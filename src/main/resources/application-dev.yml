spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enterprise_crm
    username: root
    password: asif
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"  #health,info
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
